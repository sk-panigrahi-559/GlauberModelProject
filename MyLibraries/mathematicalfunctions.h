#include <vector>

using namespace std;

// Generates random number using random engine
int rd_generate_random_integer(int, int);

// Generates a random number using random library
int rand_lib_random_integer(int, int);

// Generates a random float number
float rd_generate_random_float(float, float);

// Calculates volume of sphere
float volume_sphere(float);

// Returns the square
float square(float);

// Coordinate converter
vector<float> spherical_to_cartesian(float, float, float);

// wood saxon distribution
float wood_saxon_distribution_probability_funtion(float x);

